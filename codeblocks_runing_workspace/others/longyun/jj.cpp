/*void hh()
{   for(int i=0;i<length;i++)
      {
       p=p->next;
      // if(p->data)
      if(p->data==0&&i<nonzero)
        continue;
      else
        if(0==i&&is_ne)
            printf("-%04d",p->data);
        else printf("%04d",p->data);
       if(i!=length-1)
        printf(",");
      }
}*/
