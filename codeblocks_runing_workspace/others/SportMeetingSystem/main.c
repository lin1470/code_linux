#include <stdio.h>
#include <stdlib.h>//真的很好玩么，真的是无语了
#include "student.h"//这时候我们想要的是使用分离式的编译来使用的，非常的简单而且又是好玩的。
int main()
{
    mainmenu();
  // addProject();
    //udate_project();
   //join_project();
  // get_player_from_rand();
   // show_player();//
   // show_project();
  // show_project_in_detail();
   // creat_project_file();
 // system("pause");

    return 0;
}
